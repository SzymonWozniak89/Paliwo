{% extends 'base.html.twig' %}

{% block body %}

 <div id="intro" class="bg-image shadow-2-strong">
        <div class="mask d-flex align-items-center h-100">
            <div class="container">
                <div class="row justify-content-center">
                    <div class="col-xl-5 col-md-10">
                        <form class="bg-white rounded-5 shadow-5-strong p-5 border border-secondary" method="POST">
                            {{ form_start(carEdit) }}

                                <div class="form-control">
                                    {{ form_label(carEdit.carBrand) }}
                                    {{ form_widget(carEdit.carBrand) }}
                                    <small>{{ form_help(carEdit.carBrand) }}</small>
                                    <div class="form-error">
                                        {{ form_errors(carEdit.carBrand) }}
                                    </div>
                                </div> 
                                </br>
                                <div class="form-control">
                                    {{ form_label(carEdit.carModel) }}
                                    {{ form_widget(carEdit.carModel) }}
                                    <small>{{ form_help(carEdit.carModel) }}</small>
                                    <div class="form-error">
                                        {{ form_errors(carEdit.carModel) }}
                                    </div>
                                </div> 
                                </br>
                                <div class="form-control">
                                    {{ form_label(carEdit.carFuel) }}
                                    {{ form_widget(carEdit.carFuel) }}
                                    <small>{{ form_help(carEdit.carFuel) }}</small>
                                    <div class="form-error">
                                        {{ form_errors(carEdit.carFuel) }}
                                    </div>
                                </div>   
                                </br>
                                {{ form_row(carEdit.submit, { label: 'Zmień' }) }}

                            {{ form_end(carEdit, {render_rest: false}) }}
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <style>
    .form-control{
        border: 0;
    }
    .form-control input{
        border: 1px solid lightgrey;
    }
    </style>
{% endblock %}