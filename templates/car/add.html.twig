{% extends 'base.html.twig' %}

{% block body %}
{# {{ form(carAdd) }} #}

 <div id="intro" class="bg-image shadow-2-strong">
        <div class="mask d-flex align-items-center h-100" style="background-color: rgba(111, 111, 111, 1);">
            <div class="container">
                <div class="row justify-content-center">
                    <div class="col-xl-5 col-md-10">
                        <form class="bg-white  rounded-5 shadow-5-strong p-5" action="{{ path('car_add') }}" method="post">
                            {{ form_start(carAdd) }}
                                {{ form_row(carAdd.carBrand) }}
                                {{ form_row(carAdd.carModel) }}
                                {{ form_row(carAdd.carFuel) }}
                                {{ form_row(carAdd.carOdometer) }}
                                {# <button type="submit" class="btn btn-primary btn-block">Dodaj</button>                        #}
                            {{ form_end(carAdd) }}
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}