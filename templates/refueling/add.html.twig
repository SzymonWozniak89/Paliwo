{% extends 'base.html.twig' %}

{% block body %}

 <div id="intro" class="bg-image shadow-2-strong">
        <div class="mask d-flex align-items-center h-100">
            <div class="container">
                <div class="row justify-content-center">
                    <div class="col-xl-5 col-md-10">
                        <form class="bg-white rounded-5 shadow-5-strong p-5 border border-secondary" method="post">
                            {{ form_start(refuelingAdd) }}
                                {{ form_row(refuelingAdd.refuelingOdometer) }}
                                {{ form_row(refuelingAdd.refuelingLiters) }}
                                {{ form_row(refuelingAdd.refuelingPrice) }}
                                {{ form_row(refuelingAdd.refuelingDate) }}                       
                            {{ form_end(refuelingAdd) }}
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}